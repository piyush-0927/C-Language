# include <stdio.h>

int main () {
    // i=iterator/counter (i, j, k);
    // i++ use then increase ++i increase then use;
    for(int i=1; i<=10; i++) {
        printf("%d\n",i);
        printf("Hello!\n");   // blocks of code in statement 
    }
}